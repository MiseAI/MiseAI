{
  "name": "miseai-frontend",
  "version": "1.0.0",
 "scripts": {
  "prebuild": "if [ -f ./node_modules/.bin/vite ]; then chmod +x ./node_modules/.bin/vite; fi",
  "build": "npm run prebuild && npx vite build",
  "dev": "vite",
  "serve": "npx serve dist"
}
  "dependencies": {
    "axios": "^1.10.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.2",
    "vite": "^4.5.14",
    "serve": "^14.2.4"
  }
}
